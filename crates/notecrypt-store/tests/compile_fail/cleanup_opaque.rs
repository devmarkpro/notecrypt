use notecrypt_store::{CleanupWorkspaceId, RegisteredWorkspace, WorkspaceAbsenceProof};

fn main() {
    let _id = CleanupWorkspaceId([0_u8; 16]);
    let _registered = RegisteredWorkspace {};
    let _proof = WorkspaceAbsenceProof {};
}
